
## CUA Smoke Test - Working E2E

- **Azure endpoint**: `info-mjnxtt51-eastus2.cognitiveservices.azure.com` (NOT `vibe-dev-ai`)
- **Env file**: `~/.env.d/azure-openai.env`
- **Model**: `gpt-5.4` (vision capable, use `max_completion_tokens` not `max_tokens`)
- **Both scenarios pass**: `send_message` (7 steps) and `multi_turn` (13 steps)
- **Key fix**: Added `{"type": "send"}` action that uses uiautomator XML to find send button coords automatically. Vision models can't reliably tap the bottom-right send button (off by ~80px Y).
- **CI secrets set**: `AZURE_OPENAI_API_KEY` and `AZURE_OPENAI_ENDPOINT` on `bolajiev/clix-mobile`
- **Emulator**: AVD `test`, API 34 x86_64, at `/tmp/android-sdk/`. KVM required.
- **Connection setup**: Must dismiss keyboard before tapping Connect button (it's obscured otherwise)
- **App behavior**: Enter key inserts newline (doesn't send). Must use send button.
